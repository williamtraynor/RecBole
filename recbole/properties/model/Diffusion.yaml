n_steps: 1000
min_beta: 0.0001
max_beta: 0.02
mlp_hidden_size: [600]          # (list of int) The MLP hidden layer.
latent_dimension: 128    
dropout_cap: 0.5       # (int) The latent dimension of auto-encoder.
anneal_cap: 0.2                 # (float) The hyper parameter of the weight of KL loss. 
total_anneal_steps: 200000      # (int) The maximum steps of anneal update.

